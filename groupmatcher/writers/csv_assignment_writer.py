import csv

class CsvAssignmentWriter:
    def write(self, model_manager, output_filename=None):
        # with open(output_filename, 'wb') as csvfile:
        #     writer = csv.DictWriterwriter(csvfile)
        #
        pass